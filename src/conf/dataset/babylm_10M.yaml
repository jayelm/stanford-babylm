# @package _global_

# This config runs models on the babylm 10M dataset.

data:
  train_file: "src/data_utils/babylm_data/babylm_10M/*.train"
  validation_file: "src/data_utils/babylm_data/babylm_dev/*.dev"
  max_train_samples: 20
  block_size: 5
  max_eval_samples: 2000  # Truncate validation data for faster training.
